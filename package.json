{
  "name": "microsoft-teams-gather-status-sync",
  "version": "1.0.0",
  "description": "Detect your Microsoft Teams call status and update your availability in Gather accordingly.",
  "scripts": {
    "setup": "tsc -b && node dist/src/scripts/setup.js",
    "remove": "tsc -b && node dist/src/scripts/remove.js",
    "check": "tsc -b && node dist/src/scripts/check.js"
  },
  "author": "Manuel Pickl",
  "license": "MIT",
  "dependencies": {
    "@gathertown/gather-game-client": "^38.0.1",
    "dotenv": "^16.4.7",
    "isomorphic-ws": "^4.0.1",
    "ws": "^8.2.1"
  },
  "devDependencies": {
    "@types/node": "^16.7.10",
    "typescript": "4.3.4"
  }
}
